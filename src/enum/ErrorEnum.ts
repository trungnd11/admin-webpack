export enum ErrorMessage {
  NETWORK_ERROR = "Network Error"
};

export enum ErrorMessageVI {
  NETWORK_ERROR = "Không có kết nối, thử lại",
  TIMEOUT = "Quá thời gian kết nối, thử lại",
};

export enum ErrorCode {
  TIMEOUT = "ECONNABORTED",
  TIMEOUT_CODE = 504
};
